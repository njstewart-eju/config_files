# Sample .bashrc for SUSE Linux
# Copyright (c) SUSE Software Solutions Germany GmbH

# There are 3 different types of shells in bash: the login shell, normal shell
# and interactive shell. Login shells read ~/.profile and interactive shells
# read ~/.bashrc; in our setup, /etc/profile sources ~/.bashrc - thus all
# settings made here will also take effect in a login shell.
#
# NOTE: It is recommended to make language settings in ~/.profile rather than
# here, since multilingual X sessions would not work properly if LANG is over-
# ridden in every subshell.

test -s ~/.alias && . ~/.alias || true

# Terminal colour
# colours taken from https://github.com/shannonmoeller/vim-monokai256/blob/master/colors/monokai256.vim
directory=$(tput setaf 81);
hostname=$(tput setaf 161);
user=$(tput setaf 118);

# see also: https://github.com/CoreyMSchafer/dotfiles/blob/master/.bash_prompt
bold=$(tput bold);
reset=$(tput sgr0);
white=$(tput setaf 15);

PS1="\[${bold}\]"; 
PS1+="\[${user}\]\u"; 
PS1+="\[${white}\] @ ";
PS1+="\[${hostname}\]\h"; 
PS1+="\[${white}\] in ";
PS1+="\[${directory}\]\W"; # working directory
PS1+="\[${white}\]\$ \[${reset}\]"; # `$` (and reset color)
export PS1;

# Default text editor
export EDITOR='subl'
export VISUAL='subl'

export GTK_IM_MODULE=fcitx
export XMODIFIERS=@im=fcitx
export QT_IM_MODULE=fcitx

# Python Path
export PYTHONPATH=/home/njs/Documents/github/Python_Misc:/home/njs/Documents/github/Python_MRI

	#--------------------------------------------------
	# Start of ESE environment setup for release MR29.1_R01 .
	#--------------------------------------------------

alias ese2MR29.1_R01='source /ESE_MR29.1_R01/psd/bin/set_ese_vars_bash'
ese2MR29.1_R01

	#--------------------------------------------------
	# End of ESE environment setup for release MR29.1_R01 .
	#--------------------------------------------------




	#--------------------------------------------------
	# Start of ESE environment setup for release DV26.0_R02 .
	#--------------------------------------------------

alias ese2DV26.0_R02='source /ESE_DV26.0_R02/psd/config/set_ese_vars_bash'
ese2DV26.0_R02

	#--------------------------------------------------
	# End of ESE environment setup for release DV26.0_R02 .
	#--------------------------------------------------




	#--------------------------------------------------
	# Start of ESE environment setup for release MP26.0_R02 .
	#--------------------------------------------------

alias ese2MP26.0_R02='source /ESE_MP26.0_R02/psd/config/set_ese_vars_bash'
ese2MP26.0_R02

	#--------------------------------------------------
	# End of ESE environment setup for release MP26.0_R02 .
	#--------------------------------------------------


